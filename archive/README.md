# Archived source material

- `main_original_from_attachment.txt` is an unchanged copy of the supplied Raspberry Pi program before repository path/status edits. The documented program is `src/main.py`.
- The original workspace's `clean_images.py` deletes image files with repeated stems. It was not run. Use the repository's read-only dataset checker instead.
- The old `v1.zip`, `data_LABEL.zip`, extracted Label Studio workspace, and stock/reference image library remain in their original locations. They were not selected for public release.
